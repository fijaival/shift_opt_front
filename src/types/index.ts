export interface DriverPostResponse {
  id: number;
  message: string;
}

export interface Driver {
  id: number;
  firstName: string;
  lastName: string;
}

export interface DriverName {
  first_name: string;
  last_name: string;
}
